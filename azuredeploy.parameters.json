{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json",
    "contentVersion": "1.0.0.0",
    "parameters": {    
        "resourceNamePrefix":{
            "value": "{prefix for resource name defaults}"
        },
        "vnetSubscription": {
            "value":"{subscription of the target VNet}"
        },
        "vnetResourceGroup": {
            "value":"{respurce group of the target vnet}"
        },
        "vnetName":{
            "value":"{target vnet name}"
        },
        "wafSubNetName" : {
            "value":"{app gateway target subnet name}"
        },
        "appInboundSubNetName" : {
            "value":"{app private endpoint target subnet name}"
        },
        "appOutboundSubNetName" : {
            "value":"{app outbound target subnet name}"
        },
        "sqlSubNetName" : {
            "value":"{sql private endpoint target subnet name}"
        },        
        "ADUserObjectID" : {
            "value": "{AAD object ID. Get-AzADUser}"
        } 
    }
}